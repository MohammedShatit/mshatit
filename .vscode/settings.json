{
"liveServer.settings.https": {
    "enable": true, //set it true to enable the feature.
    "cert": "../cert.pem", //full path of the certificate
    "key": "../key.pem", //full path of the private key
    "passphrase": "12345"
    }
}